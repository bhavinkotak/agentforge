# Rust build artifacts
/target/
**/*.rs.bk

# Environment files (never commit secrets)
.env
.env.local
.env.*.local

# SQLx prepared queries (regenerate with `cargo sqlx prepare`)
# .sqlx/ is intentionally committed for offline compilation

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Docker volumes
postgres-data/

# Logs
*.log
*.jsonl

# Compiled binaries at repo root (not crate directories)
/agentforge
/agentforge-api
